{
  "name": "zendesk-search",
  "version": "0.1.0",
  "description": "Prompt user to search from input files and return the searched item ",
  "main": "src/index.js",
  "scripts": {
    "test": "jest --verbose",
    "lint": "eslint ./src/*.js ./src/common/*.js ./test/*.js ./test/fixtures/*.js",
    "search": "node -e 'require(\"./src/index\").main()'"
  },
  "author": "Shuchi Sethi",
  "license": "ISC",
  "devDependencies": {
    "eslint": "^5.6.0",
    "eslint-config-airbnb-base": "^13.1.0",
    "eslint-plugin-import": "^2.14.0",
    "eslint-plugin-jest": "^21.22.1",
    "jest": "^24.8.0",
    "jest-mock-process": "^1.2.0"
  },
  "dependencies": {
    "read-files-promise": "^1.1.1"
  }
}
